<html>
    <head>
        <link rel="stylesheet" href="./css/styles.css" />
    </head>
    <body>
        {{>header}}
        {{message}}
    </body>
    <footer>
        {{>footer}}
    </footer>
</html>